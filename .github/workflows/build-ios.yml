name: Build iOS IPA
on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install CocoaPods
        run: sudo gem install cocoapods
      - name: Install npm dependencies
        run: npm install --legacy-peer-deps
      - name: Sync Capacitor
        run: npx cap sync ios
      - name: Install Pods
        run: cd ios/App && pod install
      - name: Build unsigned IPA
        run: |
          cd ios/App
          xcodebuild \
            -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build
          mkdir -p Payload
          cp -R build/Build/Products/Release-iphoneos/App.app Payload/
          zip -r æå.ipa Payload
          ls -lh æå.ipa
      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: shuguang-ios-ipa
          path: ios/App/æå.ipa
      - name: Upload IPA to server via SCP
        run: |
          # ç¨ heredoc æ¹å¼åå¥ç§é¥ï¼ç¡®ä¿æ¢è¡ç¬¦æ­£ç¡®
          cat > ~/deploy_key << 'KEYEOF'
          ${{ secrets.SSH_PRIVATE_KEY }}
          KEYEOF
          chmod 600 ~/deploy_key
          echo "=== ç§é¥æä»¶ä¿¡æ¯ ==="
          ls -la ~/deploy_key
          echo "=== è¿æ¥ä¿¡æ¯ ==="
          echo "ä¸»æº: ${{ secrets.SERVER_HOST }}"
          echo "ç¨æ·: ${{ secrets.SERVER_USER }}"
          echo "ç«¯å£: ${{ secrets.SERVER_PORT }}"
          echo "ç®æ ç®å½: ${{ secrets.SERVER_TARGET_DIR }}"
          echo "=== åå»ºæå¡å¨ç®å½ ==="
          ssh -i ~/deploy_key \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            -o ConnectTimeout=10 \
            -p ${{ secrets.SERVER_PORT }} \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "mkdir -p ${{ secrets.SERVER_TARGET_DIR }} && echo 'ç®å½åå»ºæå'"
          echo "=== å¼å§ä¸ä¼  IPA ==="
          scp -i ~/deploy_key \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            -o ConnectTimeout=10 \
            -P ${{ secrets.SERVER_PORT }} \
            ios/App/æå.ipa \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ secrets.SERVER_TARGET_DIR }}/
          echo "â IPA ä¸ä¼ æå"
          echo "=== æå¡å¨æä»¶åè¡¨ ==="
          ssh -i ~/deploy_key \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            -o ConnectTimeout=10 \
            -p ${{ secrets.SERVER_PORT }} \
            ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }} \
            "ls -lh ${{ secrets.SERVER_TARGET_DIR }}/"
