const path = require('node:path');
const { webkit } = require(path.join(process.env.TEMP, 'warehouse-release-tools/node_modules/@playwright/test'));
(async()=>{const browser=await webkit.launch();try{const page=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true});const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto('https://youxishen.online/workspace/?guest-check=1');await page.waitForTimeout(1800);if(await page.locator('.team-login').count())throw new Error('password gate is still visible');if((await page.locator('body').innerText()).includes('暂无本地数据'))throw new Error('business data still offline');if(errors.length)throw new Error(errors.join('\n'));await page.screenshot({path:'release/gate-live.png',fullPage:true});console.log('Guest startup and authenticated business data load passed.');}finally{await browser.close();}})().catch(e=>{console.error(e.message);process.exitCode=1});
