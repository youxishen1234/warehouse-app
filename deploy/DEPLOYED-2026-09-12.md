# 下载页发布记录

- 服务器：`152.136.100.200`，站点：`youxishen.online`。
- 下载页：`https://youxishen.online/download`。
- 安装包：`https://youxishen.online/shuguang.ipa`。
- 版本：`1.0 (Build 2)`，大小：5,186,594 字节。
- 更新文件：`/opt/shuguang/public/download/index.html`、`ipa.json`、`app-icon.png`。
- Nginx：`/etc/nginx/sites-available/shuguang` 的 HTTPS server 引入 `/etc/nginx/snippets/shuguang-download.conf`。
- 备份：`/var/backups/shuguang-download-20260912-02`，包含原站点配置和原下载首页。
- `nginx -t` 成功，reload 后服务状态为 active。
- 公网下载页返回 200；IPA 返回 200、附件下载头，支持字节范围请求。
- 公网桌面 Chromium 与移动尺寸 WebKit 均完成真实 IPA 下载，文件名、大小和 SHA-256 验证通过；下载后仍停留在下载页。移动 WebKit 验证不替代实际 iPhone 网络环境验收。
- 业务首页和 IPA 的部署前后 SHA-256 一致。未发布工作区旧业务前端，未修改数据库或应用签名。
- 首次验收在 Nginx reload 后读到了旧 worker 响应，已自动回滚。第二次发布增加等待新配置生效的校验后成功。

本次使用密码进行 SSH 认证，服务器主机密钥与本机此前记录的 Ed25519 主机密钥匹配。密码及用户私钥未写入项目或部署产物。

## IP 下载入口

已在默认 HTTP 80 端口站点接入同一个 Nginx 下载片段：

- 下载页：`http://152.136.100.200/download`。
- IPA 直链：`http://152.136.100.200/shuguang.ipa`。
- IP 下载页直接返回 200，不跳转到域名；IPA 返回附件下载头。
- 本次站点配置备份：`/var/backups/shuguang-ip-download-20260912/site.conf`。
- `https://152.136.100.200` 的 TLS 证书不匹配，因为现有证书只覆盖域名；IP 地址须使用 `http://`。
